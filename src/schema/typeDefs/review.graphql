type Review {
  _id: ID!
  userId: ID!
  productId: ID!
  rating: Int!
  comment: String!
}

input AddReviewInput {
  userId: ID!
  productId: ID!
  rating: Int!
  comment: String!
}

input EditReviewInput {
  _id: ID!
  userId: ID
  productId: ID
  rating: Int
  comment: String
}
